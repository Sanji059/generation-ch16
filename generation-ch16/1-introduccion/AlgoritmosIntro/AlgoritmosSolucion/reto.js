function numero1(numero) {
    if (numero % 7 ===   0 || numero % 8 === 0 ) {
        return true;
    } else {
        return false;
    }
}
let numero = (prompt("Ingrese un numero"));
console.log (resultado = numero1(numero));